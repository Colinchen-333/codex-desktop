/**
 * Constants Index
 *
 * Centralized export point for all application constants.
 * This promotes consistency and makes it easier to maintain
 * and update values across the application.
 */

// Timeout and timing constants
export {
  POLL_INTERVALS,
  CACHE_TTL,
  TIMEOUTS,
  DELAYS,
  ANIMATION_DURATIONS,
} from './timeouts'

// UI-related constants
export {
  Z_INDEX,
  BREAKPOINTS,
  CONTAINER_SIZES,
  ICON_SIZES,
  FONT_SIZES,
  SPACING,
  BORDER_RADIUS,
  SEARCH_SCORES,
  FILE_PATH_LIMITS,
  UI_THRESHOLDS,
} from './ui'

// Animation constants
export {
  EASING,
  DURATIONS,
  DELAYS as ANIMATION_DELAYS,
  STAGGER,
  SPRINGS,
  KEYFRAMES,
  PRESETS,
  TRANSITIONS,
  REDUCED_MOTION,
} from './animations'
